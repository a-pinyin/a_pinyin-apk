import 'package:flutter/material.dart';
import '../../c/todo_page.dart';

class PageIcount extends StatelessWidget {
  const PageIcount({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const TodoPage(
      title: '输入统计',
    );
  }
}
