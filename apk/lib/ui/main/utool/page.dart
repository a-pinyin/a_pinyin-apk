import 'package:flutter/material.dart';
import '../../c/todo_page.dart';

class PageUtool extends StatelessWidget {
  const PageUtool({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const TodoPage(
      title: 'Unicode 小工具',
    );
  }
}
