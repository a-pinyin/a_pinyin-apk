import 'package:flutter/material.dart';

// `TODO` 占位符
class TodoPlaceholder extends StatelessWidget {
  const TodoPlaceholder({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('ToDo'),
    );
  }
}
